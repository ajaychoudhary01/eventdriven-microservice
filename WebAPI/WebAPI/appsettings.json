{
  "AllowedHosts": "*",
  "KafkaConfig": {
    "BootstrapServers": "localhost:9092", 
    "Username": "kafka API Key if using confluent cloud or username if local plain authentication",
    "Password": "kafka API Secret Key if using confluent cloud or password if local plain authentication"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "MongoConnectionSetting": {
    "ConnectionString": "localhost:27017",
    "DatabaseName": "Data base name",
    "ReviewCollectionName": "reviews"
  }
}
